{
  "name": "win-game-hub",
  "version": "1.0.0",
  "description": "Windows Game Emulator Environment for iOS",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "ios": "react-native run-ios",
    "build:ios": "cd ios && xcodebuild -workspace WinHub.xcworkspace -scheme WinHub -configuration Release"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-native": "0.73.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
